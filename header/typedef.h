#ifndef TYPEDEF_H
#define TYPEDEF_H

#include <utility>

namespace ic{
	typedef unsigned long int uli;
	typedef std::pair<uli, double> gen_valor;
	typedef std::pair<int, double> indice_fitness;
}

#endif
